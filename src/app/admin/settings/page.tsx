import { requireSuperAdmin } from "@/lib/admin";
export default async function Settings(){await requireSuperAdmin();return <div><h1 className="text-3xl font-black">Settings</h1><section className="mt-6 rounded-2xl border border-white/10 bg-[#101a31] p-5"><h2 className="font-black">Konfigurasi integrasi</h2><p className="mt-2 text-sm text-slate-400">Payment provider: <b>{process.env.PAYMENT_PROVIDER === "development" ? "development" : "configured"}</b></p><p className="mt-1 text-sm text-slate-400">Product provider: <b>{process.env.PRODUCT_PROVIDER === "development" ? "development" : "configured"}</b></p><p className="mt-4 text-sm text-amber-200">Secret tidak ditampilkan atau dapat diubah dari browser. Kelola melalui environment/deployment secret manager.</p></section></div>}
