import { notFound } from "next/navigation";import { requireAdminArea } from "@/lib/admin";import { prisma } from "@/lib/prisma";import { updateCategory } from "../actions";
export default async function EditCategory({params}:{params:Promise<{id:string}>}){await requireAdminArea("catalog");const {id}=await params;const category=await prisma.gameCategory.findUnique({where:{id}});if(!category)notFound();return <div><h1 className="text-3xl font-black">Edit kategori</h1><form action={updateCategory} className="mt-6 grid max-w-2xl gap-3 rounded-2xl border border-white/10 bg-[#101a31] p-5"><input name="id" type="hidden" value={category.id}/><input name="name" className="field" defaultValue={category.name} required/><input name="slug" className="field" defaultValue={category.slug} required/><button className="button">Simpan kategori</button></form></div>}
