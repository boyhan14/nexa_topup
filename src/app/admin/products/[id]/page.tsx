import { notFound } from "next/navigation"; import { requireAdminArea } from "@/lib/admin"; import { prisma } from "@/lib/prisma"; import { ProductForm } from "../product-form"; import { updateProduct } from "../actions";
export default async function EditProduct({params}:{params:Promise<{id:string}>}) { await requireAdminArea("catalog"); const {id}=await params; const [product,games]=await Promise.all([prisma.gameProduct.findUnique({where:{id},select:{id:true,gameId:true,name:true,sku:true,providerPrice:true,sellingPrice:true,isActive:true,image:true}}),prisma.game.findMany({select:{id:true,name:true},orderBy:{name:"asc"}})]); if(!product) notFound(); const productValue={...product,providerPrice:product.providerPrice.toString(),sellingPrice:product.sellingPrice.toString()}; return <div><h1 className="text-3xl font-black">Edit produk</h1><section className="mt-6 rounded-2xl border border-white/10 bg-[#101a31] p-5"><ProductForm games={games} product={productValue} action={updateProduct}/></section></div>; }
