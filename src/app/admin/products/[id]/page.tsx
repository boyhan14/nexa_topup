import { notFound } from "next/navigation"; import { requireAdminArea } from "@/lib/admin"; import { prisma } from "@/lib/prisma"; import { ProductForm } from "../page"; import { updateProduct } from "../actions";
export default async function EditProduct({params}:{params:Promise<{id:string}>}) { await requireAdminArea("catalog"); const {id}=await params; const [product,games]=await Promise.all([prisma.gameProduct.findUnique({where:{id}}),prisma.game.findMany({orderBy:{name:"asc"}})]); if(!product) notFound(); return <div><h1 className="text-3xl font-black">Edit produk</h1><section className="mt-6 rounded-2xl border border-white/10 bg-[#101a31] p-5"><ProductForm games={games} product={product} action={updateProduct}/></section></div>; }
