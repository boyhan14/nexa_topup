# Deployment

Provision PostgreSQL and configure values from `.env.example` as deployment secrets. Run migrations and controlled seeding. Configure HTTPS callbacks, logs, alerts, backups, and a queue worker. Never expose provider or payment secrets to the browser.
